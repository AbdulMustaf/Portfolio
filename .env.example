GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.0-flash

# Optional: Vercel KV / Upstash Redis REST credentials for a persistent viewer count.
# Without these, /api/viewer-count falls back to a per-function-instance counter.
KV_REST_API_URL=
KV_REST_API_TOKEN=
